Railway build notes

Use the Maven wrapper and the `production` profile to build this project.

Build command:

```bash
./railway-build.sh
```

If Railway runs from the repository root, this script will use the included `./mvnw` wrapper and the `production` profile.
